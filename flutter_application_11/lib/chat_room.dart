import 'package:flutter/material.dart';

class RoomChat extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text(
          "Apakah kamu sudah mengerjakan tugasmu hari ini? Kalau sudah silakan kumpulkan ke surel saya siang ini"),
    );
  }
}
